#include <stdio.h>
/**
 * main -Positive anything is better than negative nothing
 * Return: 0
 */

int main(void)
{
	int ab;

	for (ab = 122; ab >= 97; ab--)
	{
		putchar(ab);
	}
	putchar('\n');

	return (0);
}
