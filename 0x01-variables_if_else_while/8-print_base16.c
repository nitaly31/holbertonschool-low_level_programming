#include <stdio.h>
/**
 * main -Positive anything is better than negative nothing
 * Return: 0
 */

int main(void)
{
	int ab;

	for (ab = 48; ab <= 57; ab++)
	{
		putchar(ab);
	}
	for (ab = 97; ab <= 102; ab++)
	{
		putchar(ab);
	}
	putchar('\n');

	return (0);
}
